#pragma once

/**
 * // Q1 / Q1.1
    "SELECT SUM(LO_EXTENDEDPRICE*LO_DISCOUNT) AS REVENUE\n"
    "   FROM HAT.LINEORDER, HAT.DATE\n"
    "   WHERE LO_ORDERDATE = D_DATEKEY\n"
    "   AND D_YEAR = 1993\n"
    "   AND LO_DISCOUNT BETWEEN 1 AND 3\n"
    "   AND LO_QUANTITY<25",
    // Q2 / Q1.2
    "SELECT SUM(LO_EXTENDEDPRICE*LO_DISCOUNT) AS REVENUE\n"
    "   FROM HAT.LINEORDER, HAT.DATE\n"
    "   WHERE LO_ORDERDATE = D_DATEKEY\n"
    "   AND D_YEARMONTHNUM = 199401\n"
    "   AND LO_DISCOUNT BETWEEN 4 AND 6\n"
    "   AND LO_QUANTITY BETWEEN 26 AND 35",
    // Q3 / Q1.3
    "SELECT SUM(LO_EXTENDEDPRICE*LO_DISCOUNT) AS REVENUE\n"
    "   FROM HAT.LINEORDER, HAT.DATE\n"
    "   WHERE LO_ORDERDATE = D_DATEKEY\n"
    "   AND D_WEEKNUMINYEAR = 6\n"
    "   AND D_YEAR = 1994\n"
    "   AND LO_DISCOUNT BETWEEN 5 AND 7\n"
    "   AND LO_QUANTITY BETWEEN 26 AND 35",
 */


// Index required for Q1.1: LO_ORDERDATE, D_DATEKEY, D_YEAR
// Index required for Q1.2: LO_ORDERDATE, D_DATEKEY, D_YEARMONTHNUM
// Index required for Q1.3: 
// Index required for NewOrder: C_NAME, P_PARTKEY, S_NAME, D_DATE
// Index required for Payment: C_NATION, C_CUSTKEY, S_SUPPKEY
// Index required for CountOrder: LO_CUSTKEY

/**
 * Q1.1 改成 "SELECT SUM(LO_EXTENDEDPRICE * LO_DISCOUNT) AS REVENUE\n"
"   FROM HAT.LINEORDER, HAT.DATE\n"
"   WHERE LO_ORDERDATE = D_DATEKEY\n"
"   AND D_DATEKEY >= 19930101 AND D_DATEKEY <= 19931231\n"
"   AND LO_ORDERDATE >= 19930101 AND LO_ORDERDATE <= 19931231\n"
"   AND LO_DISCOUNT BETWEEN 1 AND 3\n"
"   AND LO_QUANTITY < 25",
 */

/**
 * Q1.2 改成 "SELECT SUM(LO_EXTENDEDPRICE * LO_DISCOUNT) AS REVENUE\n"
"   FROM HAT.LINEORDER, HAT.DATE\n"
"   WHERE LO_ORDERDATE = D_DATEKEY\n"
"   AND D_DATEKEY BETWEEN 19940101 AND 19940131\n"
"   AND LO_DISCOUNT BETWEEN 4 AND 6\n"
"   AND LO_QUANTITY BETWEEN 26 AND 35",
 */